apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- k6-job.yaml

configMapGenerator:
- name: k6-load-test
  namespace: dev
  files:
  - load-test.js

generatorOptions:
  disableNameSuffixHash: true
