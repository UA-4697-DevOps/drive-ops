apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "client-gateway.name" . }}-sa
  labels:
    {{- include "client-gateway.labels" . | nindent 4 }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.serviceAccount.iamRoleArn }}
