apiVersion: v1
kind: Service
metadata:
  name: {{ include "client-gateway.name" . }}
  labels:
    {{- include "client-gateway.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "client-gateway.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
