apiVersion: v1
kind: Secret
metadata:
  name: {{ include "client-gateway.name" . }}-secrets
  labels:
    {{- include "client-gateway.labels" . | nindent 4 }}
type: Opaque
stringData:
  bot-token: {{ .Values.bot.token | quote }}
