apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "driver-service.name" . }}-sa
  labels:
    {{- include "driver-service.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
