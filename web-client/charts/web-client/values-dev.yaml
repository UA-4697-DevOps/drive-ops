# ---------------------------------------------------------------------------
# Dev environment overrides for web-client Helm chart.
# Used by helm-dev-deploy.sh and local kind cluster testing.
# ---------------------------------------------------------------------------

image:
  pullPolicy: Never   # Don't pull from registry; image was loaded directly into node

labels:
  env: dev
