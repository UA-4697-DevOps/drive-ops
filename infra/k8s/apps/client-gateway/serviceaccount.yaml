apiVersion: v1
kind: ServiceAccount
metadata:
  name: client-gateway-sa
  namespace: client-gateway
  labels:
    app: client-gateway
  annotations:
    eks.amazonaws.com/role-arn: ""  # Environment-specific ARN to be injected via deployment overlay or value
