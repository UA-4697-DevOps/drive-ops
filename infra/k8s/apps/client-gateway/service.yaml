apiVersion: v1
kind: Service
metadata:
  name: client-gateway
  namespace: client-gateway
  labels:
    app: client-gateway
spec:
  selector:
    app: client-gateway
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
