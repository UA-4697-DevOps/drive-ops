---
# Default deny all ingress to the web-client namespace.
# Explicit allow rules below open only the ports that are required.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: web-client-default-deny-ingress
  namespace: web-client
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
# Allow ingress to web-client from any source (user-facing service).
# The ALB (target-type=ip) forwards requests directly to pod IPs on port 8083.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: web-client-allow-ingress
  namespace: web-client
spec:
  podSelector:
    matchLabels:
      app: web-client
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: 8083
---
# Allow egress from web-client to driver-service namespace
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: web-client-egress-driver-service
  namespace: web-client
spec:
  podSelector:
    matchLabels:
      app: web-client
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: driver-service
      ports:
        - protocol: TCP
          port: 80
---
# Allow egress to kube-dns (required for service discovery)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: web-client-egress-dns
  namespace: web-client
spec:
  podSelector:
    matchLabels:
      app: web-client
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
