apiVersion: v1
kind: ServiceAccount
metadata:
  name: web-client-sa
  namespace: web-client
  labels:
    app: web-client
  annotations: {}
  # No IRSA annotation needed — web-client serves static files and proxies
  # to driver-service. It does not access any AWS resources directly.
