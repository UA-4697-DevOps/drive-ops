apiVersion: v1
kind: Service
metadata:
  name: web-client
  namespace: web-client
  labels:
    app: web-client
spec:
  selector:
    app: web-client
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP
