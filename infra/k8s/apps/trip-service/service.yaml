apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: dev
  labels:
    app: trip-service
spec:
  selector:
    app: trip-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8081
  type: ClusterIP