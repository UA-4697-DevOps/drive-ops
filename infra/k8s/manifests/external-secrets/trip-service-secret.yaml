# Canonical ExternalSecret for trip-service-secrets.
# Keep a single writer for this target Secret to avoid ESO ownership conflicts.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: trip-service-secrets
  namespace: trip-service
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: trip-service-secrets
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        DB_URL: "postgresql://{{ .DB_USER }}:{{ .DB_PASSWORD }}@{{ .RDS_ENDPOINT }}/trip_db"
  data:
    - secretKey: DB_USER
      remoteRef:
        key: drive-ops/dev/rds/credentials
        property: username
    - secretKey: DB_PASSWORD
      remoteRef:
        key: drive-ops/dev/rds/credentials
        property: password
    - secretKey: RDS_ENDPOINT
      remoteRef:
        key: drive-ops/dev/trip-service
        property: RDS_ENDPOINT
