apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: trip-service
  namespace: monitoring
  labels:
    release: monitoring-stack
spec:
  namespaceSelector:
    matchNames:
      - trip-service
  selector:
    matchLabels:
      app: trip-service
  jobLabel: app
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
