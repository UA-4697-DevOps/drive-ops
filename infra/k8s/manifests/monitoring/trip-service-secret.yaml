# --- Trip Service Database Secret ---
# In production, this is populated by the ExternalSecret operator
# (trip-service-secrets in trip-service namespace).
# This placeholder exists only for the monitoring stack's reference.
apiVersion: v1
kind: Secret
metadata:
  name: trip-service-db
  namespace: trip-service
  labels:
    app: trip-service
type: Opaque
stringData:
  db-url: "placeholder-see-external-secrets"
