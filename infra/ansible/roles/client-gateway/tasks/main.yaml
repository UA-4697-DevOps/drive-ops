---
# Tasks for Client Gateway (Python-based Telegram Bot)

- name: Start Client Gateway service
  community.docker.docker_compose_v2:
    project_src: "/opt/drive-ops"
    state: present
    services:
      - "client-gateway"
    build: "{{ docker_build_strategy | default('policy') }}"
  tags: [ app, gateway ]
