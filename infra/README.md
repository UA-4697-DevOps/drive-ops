## Local Infrastructure
To start the database and message broker:
1. Copy the example environment file: `cp .env.example .env`
2. Run the stack: `docker-compose up -d`
3. RabbitMQ UI: http://localhost:15672 (guest/guest)
