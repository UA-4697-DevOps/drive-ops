# Guide

## Branching
- Use `feature/` for new tasks.
- Use `fix/` for bug fixes.

## Commits
- Use [Conventional Commits](https://www.conventionalcommits.org/) (e.g., `feat:`, `fix:`, `docs:`).

## Pull Requests
- PRs require at least 1 review.
- Main branch is protected.